"use client";

import { AlertTriangle, BookOpenCheck, CheckCircle2, Eye, FileEdit, LoaderCircle, Plus, Search, Trash2, Upload, UserRound } from "lucide-react";
import { useEffect, useMemo, useState } from "react";
import { createRevisedOperationsProcedure, deleteOperationsProcedure, saveOperationsProcedure } from "@/app/operations/actions";
import type { OperationsProcedureRecord } from "@/lib/operations/data";
import type { OperationsPersistence } from "@/lib/operations/repository";
import { readOperationsProcedures, writeOperationsProcedures } from "@/lib/operations/storage";

const categories: Array<"All categories" | OperationsProcedureRecord["category"]> = ["All categories", "Delivery", "Sales floor", "Store operations", "Service", "Safety"];

export function OperationsProcedureManager({ initialProcedures = [], persistence = "demo", initialError = "", canManage = true }: { initialProcedures?: OperationsProcedureRecord[]; persistence?: OperationsPersistence; initialError?: string; canManage?: boolean }) {
  const [procedures, setProcedures] = useState<OperationsProcedureRecord[] | null>(persistence === "supabase" ? initialProcedures : null);
  const [error, setError] = useState(initialError); const [message, setMessage] = useState(""); const [search, setSearch] = useState("");
  const [categoryFilter, setCategoryFilter] = useState<(typeof categories)[number]>("All categories"); const [selectedId, setSelectedId] = useState<string | null>(null); const [editingId, setEditingId] = useState<string | null>(null); const [creationMode, setCreationMode] = useState<"manual" | "document">("manual"); const [uploading, setUploading] = useState(false);
  const [title, setTitle] = useState(""); const [category, setCategory] = useState<OperationsProcedureRecord["category"]>("Store operations"); const [owner, setOwner] = useState(""); const [summary, setSummary] = useState(""); const [stepsText, setStepsText] = useState(""); const [status, setStatus] = useState<OperationsProcedureRecord["status"]>("Draft");

  useEffect(() => { if (persistence === "supabase") return; const timer = window.setTimeout(() => { try { setProcedures(readOperationsProcedures()); } catch { setError("Saved procedures could not be loaded from this browser."); setProcedures([]); } }, 0); return () => window.clearTimeout(timer); }, [persistence]);
  const filtered = useMemo(() => (procedures ?? []).filter((procedure) => (categoryFilter === "All categories" || procedure.category === categoryFilter) && `${procedure.title} ${procedure.summary} ${procedure.owner}`.toLowerCase().includes(search.trim().toLowerCase())), [procedures, search, categoryFilter]);
  const selected = (procedures ?? []).find((procedure) => procedure.id === selectedId) ?? null;
  function saveLibrary(next: OperationsProcedureRecord[]) { try { writeOperationsProcedures(next); setProcedures(next); setError(""); } catch { setError("Procedure changes could not be saved in this browser."); } }
  function clearEditor() { setEditingId(null); setCreationMode("manual"); setTitle(""); setCategory("Store operations"); setOwner(""); setSummary(""); setStepsText(""); setStatus("Draft"); setError(""); }
  function editProcedure(procedure: OperationsProcedureRecord) { setSelectedId(procedure.id); setEditingId(procedure.id); setCreationMode("manual"); setTitle(procedure.title); setCategory(procedure.category); setOwner(procedure.owner); setSummary(procedure.summary); setStepsText(procedure.steps.join("\n")); setStatus(procedure.status); }
  async function saveProcedure(event: React.FormEvent<HTMLFormElement>) { event.preventDefault(); const steps = stepsText.split("\n").map((step) => step.trim()).filter(Boolean); if (title.trim().length < 2 || owner.trim().length < 2 || summary.trim().length < 10 || !steps.length) { setError("Add a title, owner, useful summary, and at least one procedure step."); return; } const existing = procedures?.find((procedure) => procedure.id === editingId); let record: OperationsProcedureRecord = { id: existing?.id ?? `new-${crypto.randomUUID()}`, title: title.trim(), category, owner: owner.trim(), summary: summary.trim(), steps, status, version: existing ? existing.version + 1 : 1, updatedAt: new Date().toISOString() }; if (persistence === "supabase") { const result = await saveOperationsProcedure(record); if (result.error || !result.record) { setError(result.error ?? "Procedure could not be saved."); return; } record = result.record; setProcedures([record, ...(procedures ?? []).filter((procedure) => procedure.id !== record.id)]); } else saveLibrary([record, ...(procedures ?? []).filter((procedure) => procedure.id !== record.id)]); setSelectedId(record.id); setEditingId(record.id); setError(""); setMessage("Procedure saved."); }
  async function removeSelected() { if (!selected || !confirm(`Delete “${selected.title}”? This cannot be undone.`)) return; if (persistence === "supabase") { const result = await deleteOperationsProcedure(selected.id); if (result.error) { setError(result.error); return; } } const next = (procedures ?? []).filter((procedure) => procedure.id !== selected.id); if (persistence === "demo") saveLibrary(next); else setProcedures(next); setSelectedId(null); clearEditor(); setMessage("Procedure deleted."); }
  async function reviseFromUpload(event: React.FormEvent<HTMLFormElement>) { event.preventDefault(); if (persistence !== "supabase") { setError("Document revision is available in the shared workspace. Sign in to use it."); return; } setUploading(true); setError(""); setMessage(""); const result = await createRevisedOperationsProcedure(new FormData(event.currentTarget)); setUploading(false); if (result.error || !result.record) { setError(result.error ?? "The revised procedure could not be created."); return; } setProcedures((current) => [result.record!, ...(current ?? [])]); editProcedure(result.record); setMessage("A revised draft procedure was created. Review, edit, and publish it when ready."); }

  if (procedures === null && !error) return <div className="card operations-loading"><LoaderCircle className="spin" size={22}/><div><h2>Loading procedure library</h2><p>Checking this browser for saved procedures.</p></div></div>;
  const editor = selected && !editingId ? <><div className="metric-row"><div><span className={`badge ${selected.status === "Published" ? "" : "amber"}`}>{selected.status}</span><h2>{selected.title}</h2></div><Eye size={18}/></div><p>{selected.summary}</p><p><strong>Owner:</strong> {selected.owner} · <strong>Category:</strong> {selected.category} · Version {selected.version}</p><ol>{selected.steps.map((step, index) => <li key={`${selected.id}-${index}`}>{step}</li>)}</ol>{canManage && <div className="button-row"><button className="btn btn-secondary" type="button" onClick={() => editProcedure(selected)}><FileEdit size={16}/> Edit</button><button className="btn btn-danger" type="button" onClick={removeSelected}><Trash2 size={16}/> Delete</button></div>}</> : canManage ? <><div className="metric-row"><div><span className="badge blue">Manager editor</span><h2>{editingId ? "Edit procedure" : "Create a procedure"}</h2></div><FileEdit size={18}/></div><p>{editingId ? "Saving creates the next version while retaining the procedure identity." : "Choose how you want to start this procedure."}</p>{!editingId && persistence === "supabase" && <div className="operations-creation-methods" role="tablist" aria-label="Procedure creation method"><button type="button" className={`btn ${creationMode === "manual" ? "btn-primary" : "btn-ghost"}`} role="tab" aria-selected={creationMode === "manual"} onClick={() => { setCreationMode("manual"); setError(""); }}>Create manually</button><button type="button" className={`btn ${creationMode === "document" ? "btn-primary" : "btn-ghost"}`} role="tab" aria-selected={creationMode === "document"} onClick={() => { setCreationMode("document"); setError(""); }}><Upload size={16}/> Create from document</button></div>}{!editingId && persistence === "supabase" && creationMode === "document" ? <form className="form-stack" onSubmit={reviseFromUpload} noValidate><label><span className="label">Upload a procedure document</span><input className="input file-input" name="file" type="file" required accept=".pdf,.docx,.md,.txt,application/pdf,text/plain,text/markdown,application/vnd.openxmlformats-officedocument.wordprocessingml.document"/><small className="field-help">PDF, DOCX, Markdown, or text · maximum 4 MB</small></label><p className="field-help">RunFloor will create a clean, revised draft. You can edit every field before publishing.</p><button className="btn btn-primary" disabled={uploading}>{uploading ? <><LoaderCircle className="spin" size={16}/> Creating revised draft…</> : <><Upload size={16}/> Upload and create revised draft</>}</button></form> : <form className="form-stack" onSubmit={saveProcedure}><label><span className="label">Procedure title</span><input className="input" required value={title} onChange={(event) => setTitle(event.target.value)}/></label><div className="grid grid-2"><label><span className="label">Category</span><select className="input" value={category} onChange={(event) => setCategory(event.target.value as OperationsProcedureRecord["category"])}>{categories.slice(1).map((item) => <option key={item}>{item}</option>)}</select></label><label><span className="label">Owner</span><input className="input" required value={owner} onChange={(event) => setOwner(event.target.value)} placeholder="Person or role"/></label></div><label><span className="label">Purpose and scope</span><textarea className="input" required rows={3} value={summary} onChange={(event) => setSummary(event.target.value)}/></label><label><span className="label">Procedure steps</span><textarea className="input" required rows={7} value={stepsText} onChange={(event) => setStepsText(event.target.value)}/><small className="field-help">Enter one step per line.</small></label><label><span className="label">Publishing status</span><select className="input" value={status} onChange={(event) => setStatus(event.target.value as OperationsProcedureRecord["status"])}><option>Draft</option><option>Published</option></select></label><button className="btn btn-primary" type="submit"><CheckCircle2 size={16}/> {editingId ? "Save next version" : "Create procedure"}</button></form>}{editingId && <button className="text-button operations-editor-cancel" type="button" onClick={() => { clearEditor(); setSelectedId(null); }}>Cancel editing</button>}</> : <div><Eye size={24}/><h2>Select a procedure</h2><p>Choose a procedure to view its purpose and steps.</p></div>;
  return <div className="operations-procedure-workspace"><section><div className="operations-procedure-toolbar"><label><span className="label">Search procedures</span><span className="operations-search-input"><Search size={16}/><input className="input" value={search} onChange={(event) => setSearch(event.target.value)} placeholder="Search title, owner, or purpose"/></span></label><label><span className="label">Category</span><select className="input" value={categoryFilter} onChange={(event) => setCategoryFilter(event.target.value as (typeof categories)[number])}>{categories.map((item) => <option key={item}>{item}</option>)}</select></label>{canManage && <button className="btn btn-primary" type="button" onClick={clearEditor}><Plus size={16}/> New procedure</button>}</div>{error && <p className="form-error operations-procedure-error"><AlertTriangle size={14}/>{error}</p>}{message && <p className="form-success">{message}</p>}<div className="section-heading operations-list-heading"><div><h2>Procedure library</h2><p>{filtered.length} of {procedures?.length ?? 0} procedures</p></div></div>{filtered.length ? <div className="operations-procedure-cards">{filtered.map((procedure) => <button className={`card operations-procedure-card ${selectedId === procedure.id ? "selected" : ""}`} key={procedure.id} type="button" onClick={() => { setSelectedId(procedure.id); setEditingId(null); }}><div className="metric-row"><span className={`badge ${procedure.status === "Published" ? "" : "amber"}`}>{procedure.status}</span><small>Version {procedure.version}</small></div><span className="metric-icon"><BookOpenCheck size={18}/></span><h2>{procedure.title}</h2><p>{procedure.summary}</p><div className="operations-procedure-card-meta"><span>{procedure.category}</span><span><UserRound size={13}/>{procedure.owner}</span></div></button>)}</div> : <div className="card output empty"><div><BookOpenCheck size={28}/><h2>No matching procedures</h2><p>Change the filters or create a new procedure.</p></div></div>}</section><aside className="card operations-procedure-editor">{editor}</aside></div>;
}
